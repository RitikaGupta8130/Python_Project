This project uses Python 3.x. The required dependencies are listed in requirements.txt. 

You can install them using the command mentioned in the "Setup and Usage" section.

pip install -r requirements.txt
